<h1>Here are the students</h1>
<% if @students.any? %>
  <ul>
  <% @students.each do |student| %>
    <li><%= link_to student.name, student_path(student) %></li>
  <% end %>
  </ul>
<% else %>
  No students found!
<% end %>

